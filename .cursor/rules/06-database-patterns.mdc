---
description: Database patterns — Drizzle ORM conventions, schema design, driver support
globs: ["packages/schema/**", "packages/core/src/database.ts", "plugins/**"]
alwaysApply: false
---

# Database Patterns — Drizzle ORM

> **Khi thêm DB driver mới hoặc thay đổi schema patterns → cập nhật file này.**

## Driver Support (hiện tại)

| Driver | Status | Config |
|---|---|---|
| `sqlite` | ✅ Production-ready | `{ driver: 'sqlite', url: './data.db' }` |
| `turso` | 🔜 Planned | `{ driver: 'turso', url: 'libsql://...', authToken: '...' }` |
| `postgres` | 🔜 Planned | `{ driver: 'postgres', url: 'postgresql://...' }` |

Source: `packages/core/src/database.ts` — thêm driver mới ở hàm `createDatabase()`

SQLite được tối ưu với: `PRAGMA journal_mode = WAL` (set tự động khi khởi tạo)

## Schema Architecture

### Core Tables (`packages/schema/`)

```
sites              → site registry (slug, status, config)
site_domains       → domain mapping cho site (primary/verified)
users              → platform user identity (global)
user_site_roles    → role assignment theo site
sessions           → user sessions (site-scoped qua `site_id`)
installed_plugins  → plugins đã cài theo site
installed_themes   → themes đã cài theo site
theme_state        → active theme + settings theo site (`site_id`)
```

### Plugin Tables

- Mỗi plugin export `schema` object chứa Drizzle table definitions
- Table name **bắt buộc phải có prefix** của plugin tên để tránh collision:

```typescript
// ✅ Đúng
const posts      = sqliteTable('blog_posts', ...)      // prefix: blog_
const categories = sqliteTable('blog_categories', ...) // prefix: blog_

// ❌ Sai — tên chung có thể conflict với plugin khác
const posts = sqliteTable('posts', ...)
```

### Schema Merge Flow

```
HanaCMS.launch()
  └─ createDatabase(config, pluginSchemas)
       └─ mergeSchemas(coreSchema, ...pluginSchemas)
            ├─ Throw nếu có key trùng nhau
            └─ Return merged schema object → pass vào drizzle()
```

Source: `packages/schema/src/schema-builder.ts`

## Conventions khi viết Drizzle Schema

```typescript
import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core'

export const blogPosts = sqliteTable('blog_posts', {
  // ID: integer autoIncrement
  id: integer('id').primaryKey({ autoIncrement: true }),

  // Timestamps: luôn thêm createdAt, updatedAt
  createdAt: integer('created_at', { mode: 'timestamp' })
    .notNull()
    .$defaultFn(() => new Date()),
  updatedAt: integer('updated_at', { mode: 'timestamp' })
    .notNull()
    .$defaultFn(() => new Date()),

  // Text fields
  title: text('title').notNull(),
  slug:  text('slug').notNull().unique(),
}, (table) => ({
  // Index quan trọng
  slugIdx: index('blog_posts_slug_idx').on(table.slug),
}))
```

## Multi-site Tenancy Convention (Phase 2)

- Các bảng mang trạng thái theo site **bắt buộc** có `site_id`:
  - `sessions`, `installed_plugins`, `installed_themes`, `theme_state`, plugin content tables.
- Với field từng là global unique (ví dụ `name`, `slug`), chuyển sang composite unique theo site:
  - `unique(site_id, name)` hoặc `unique(site_id, slug)`.
- `users` giữ global identity; phân quyền theo site đặt ở `user_site_roles`.
- Backward compatibility cho single-site:
  - bootstrap `site_id = 'default'` để backfill dữ liệu cũ trước khi enforce đầy đủ runtime tenancy.
- Ưu tiên index cho hot path multi-tenant:
  - `(site_id, slug)`, `(site_id, name)`, `(site_id, active_theme_name)`.

## Lấy DB Instance trong Plugin

```typescript
// ✅ Đúng — nhận qua cms:init hook
let db: ReturnType<typeof import('@hana/core').createDatabase>

export const hooks = {
  'cms:init': async (cms) => {
    db = cms.getDatabase()
  }
}

// ❌ Sai — KHÔNG tự tạo connection riêng trong plugin
import { Database } from 'bun:sqlite'
const db = new Database('./data.db')  // ❌
```

## Migration

```bash
# Chạy migration qua CLI
bunx hanabi migrate

# Lệnh này: thu thập schemas từ core + tất cả installed plugins
# → generate Drizzle migration files → apply vào DB
```

Migration được quản lý bởi `packages/cli/src/commands/migrate.ts`

## Last updated

2026-03-30 — Added multisite tenancy model (`sites`, `site_domains`, `user_site_roles`) and `site_id` scoping conventions
2026-03-22 — Added installed_themes and theme_state core tables
