---
description: Database patterns — Drizzle ORM conventions, schema design, driver support
globs: ["packages/schema/**", "packages/core/src/database.ts", "packages/core/src/database-bootstrap.ts", "packages/core/src/libsql-connection.ts", "plugins/**"]
alwaysApply: false
---

# Database Patterns — Drizzle ORM

> **Khi thêm DB driver mới hoặc thay đổi schema patterns → cập nhật file này.**

## Driver support — hai nhóm (Drizzle)

Schema trong repo hiện dùng **`drizzle-orm/sqlite-core`** (`sqliteTable`, …). Điều này **không** áp dụng trực tiếp cho adapter Postgres/MySQL; muốn hai engine đó cần **`pgTable` / `mysqlTable`** (hoặc chiến lược multi-dialect riêng).

### Nhóm A — SQLite dialect (một schema hiện tại)

| Driver | Status | Config |
|---|---|---|
| `sqlite` | ✅ Wired | Local only: `{ driver: 'sqlite', url: './data.db' }` hoặc `:memory:` — runtime **`@libsql/client` + `drizzle-orm/libsql`** (URL chuẩn hóa `file:`). |
| `turso` | ✅ Wired | `{ driver: 'turso', url: 'libsql://...', authToken: '...' }` — remote bắt buộc `authToken`; `file:` không cần. Cùng stack libSQL với `sqlite`. |

- **Không còn `drizzle-orm/bun-sqlite`** trong factory: dev và prod cùng engine libSQL, cùng `database-bootstrap.ts`.
- `PRAGMA journal_mode = WAL` — best-effort sau khi mở client (local file thường áp dụng; remote có thể bỏ qua).

### Nhóm B — Server SQL (config sẵn, schema chưa)

| Driver | Status | Config |
|---|---|---|
| `postgres` | 🔜 Cần dialect + factory | `{ driver: 'postgres', url: 'postgresql://...' }` |
| `mysql` | 🔜 Cần dialect + factory | `{ driver: 'mysql', url: 'mysql://...' }` |

`DatabaseConfig` đã cho phép các giá trị trên để deploy chỉ đổi env khi team triển khai lớp schema B; `createDatabase()` phải báo lỗi rõ nếu chưa implement.

Source: `packages/core/src/database.ts` — `export async function createDatabase()` (bootstrap libSQL là async; luôn `await` khi gọi trực tiếp).

URL resolution: `packages/core/src/libsql-connection.ts` (`toLibsqlLocalUrl`, `resolveLibsqlConnection`). `:memory:` → `file::memory:?cache=private` (in-memory tách theo client; `cache=shared` chỉ khi config URL tường minh).

## Schema Architecture

### Core Tables (`packages/schema/`)

```
sites              → site registry (slug, status, config)
site_domains       → domain mapping cho site (primary/verified)
users              → platform user identity (global)
user_site_roles    → role assignment theo site
sessions           → user sessions (site-scoped qua `site_id`)
installed_plugins  → plugins đã cài theo site
installed_themes   → themes đã cài theo site
theme_state        → active theme + settings theo site (`site_id`)
```

### Plugin Tables

- Mỗi plugin export `schema` object chứa Drizzle table definitions
- Table name **bắt buộc phải có prefix** của plugin tên để tránh collision:

```typescript
// ✅ Đúng
const posts      = sqliteTable('blog_posts', ...)      // prefix: blog_
const categories = sqliteTable('blog_categories', ...) // prefix: blog_

// ❌ Sai — tên chung có thể conflict với plugin khác
const posts = sqliteTable('posts', ...)
```

- **Plugin content tables phải có `site_id`** (plain text, default `'default'`, không dùng `.references()` vì plugin không được import `@hana/schema`):

```typescript
// ✅ Đúng — plugin content table with site_id
export const posts = sqliteTable(
  'blog_posts',
  {
    id: text('id').primaryKey(),
    siteId: text('site_id').notNull().default('default'),
    slug: text('slug').notNull(),
    // ...
  },
  (table) => ({
    siteSlugUnique: uniqueIndex('blog_posts_site_slug_unique').on(table.siteId, table.slug),
  }),
)

// ❌ Sai — không import @hana/schema trong plugin (vi phạm dependency rules)
import { sites } from '@hana/schema'
siteId: text('site_id').references(() => sites.id)  // ❌
```

### Schema Merge Flow

```
HanaCMS.launch()
  └─ await createDatabase(config, pluginSchemas)
       └─ mergeSchemas(coreSchema, ...pluginSchemas)
            ├─ Throw nếu có key trùng nhau
            └─ Return merged schema object → pass vào drizzle()
```

Source: `packages/schema/src/schema-builder.ts`

## Conventions khi viết Drizzle Schema

```typescript
import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core'

export const blogPosts = sqliteTable('blog_posts', {
  // ID: integer autoIncrement
  id: integer('id').primaryKey({ autoIncrement: true }),

  // Timestamps: luôn thêm createdAt, updatedAt
  createdAt: integer('created_at', { mode: 'timestamp' })
    .notNull()
    .$defaultFn(() => new Date()),
  updatedAt: integer('updated_at', { mode: 'timestamp' })
    .notNull()
    .$defaultFn(() => new Date()),

  // Text fields
  title: text('title').notNull(),
  slug:  text('slug').notNull().unique(),
}, (table) => ({
  // Index quan trọng
  slugIdx: index('blog_posts_slug_idx').on(table.slug),
}))
```

## Multi-site Tenancy Convention (Phase 2)

- Các bảng mang trạng thái theo site **bắt buộc** có `site_id`:
  - `sessions`, `installed_plugins`, `installed_themes`, `theme_state`, plugin content tables.
- Với field từng là global unique (ví dụ `name`, `slug`), chuyển sang composite unique theo site:
  - `unique(site_id, name)` hoặc `unique(site_id, slug)`.
- `users` giữ global identity; phân quyền theo site đặt ở `user_site_roles`.
- Backward compatibility cho single-site:
  - bootstrap `site_id = 'default'` để backfill dữ liệu cũ trước khi enforce đầy đủ runtime tenancy.
- Ưu tiên index cho hot path multi-tenant:
  - `(site_id, slug)`, `(site_id, name)`, `(site_id, active_theme_name)`.

## Lấy DB Instance trong Plugin

```typescript
// ✅ Đúng — nhận qua cms:init hook (createDatabase là async; instance lấy từ cms)
let db: Awaited<ReturnType<typeof import('@hana/core').createDatabase>>

export const hooks = {
  'cms:init': async (cms) => {
    db = cms.getDatabase()
  }
}

// ❌ Sai — KHÔNG tự tạo connection riêng trong plugin
import { Database } from 'bun:sqlite'
const db = new Database('./data.db')  // ❌
```

## Migration

```bash
# Chạy migration qua CLI
bunx hanabi migrate

# Lệnh này: thu thập schemas từ core + tất cả installed plugins
# → generate Drizzle migration files → apply vào DB
```

Migration được quản lý bởi `packages/cli/src/commands/migrate.ts`

## Last updated

2026-04-03 — `sqlite` + `turso` đều qua `@libsql/client` + `drizzle-orm/libsql` (bỏ `bun-sqlite` trong factory)
2026-04-03 — Turso/libSQL wired; `createDatabase` async; bootstrap dùng chung `database-bootstrap.ts`
2026-04-03 — Driver matrix: nhóm A (sqlite/turso) vs nhóm B (postgres/mysql); caveat schema dialect
2026-04-03 — Added plugin content table `site_id` convention (plain text, no `.references()`, site-scoped unique indexes)
2026-03-30 — Added multisite tenancy model (`sites`, `site_domains`, `user_site_roles`) and `site_id` scoping conventions
2026-03-22 — Added installed_themes and theme_state core tables
