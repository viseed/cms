{
  "name": "my-hanabi-project",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@hanabi/core": "workspace:*",
    "@hanabi/plugin-auth": "workspace:*",
    "@hanabi/plugin-blog": "workspace:*"
  }
}
