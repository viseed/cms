{
  "name": "hana-cms",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "packages/*",
    "plugins/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:plugins": "turbo run build --filter=\"./plugins/*\"",
    "dev:starter": "turbo run dev --filter=@hana/starter",
    "dev:docs": "turbo run dev --filter=@hana/docs",
    "dev:admin": "turbo run dev --filter=@hana/admin",
    "dev:playground": "turbo run dev --filter=@hana/playground",
    "test": "turbo run test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "publish-packages": "changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.7",
    "@changesets/cli": "^2.30.0",
    "bunup": "^0.16.31",
    "turbo": "^2.8.17",
    "typescript": "^5.9.3"
  }
}