{
  "name": "@viseed/schema",
  "version": "0.3.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "bun": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "bunup --no-dts && tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "dev": "bunup --no-dts --watch",
    "dev:post": "tsc --watch -p tsconfig.build.json",
    "test": "bun test"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.0"
  },
  "devDependencies": {
    "@viseed/config": "0.1.0",
    "@viseed/types": "0.3.0"
  }
}
